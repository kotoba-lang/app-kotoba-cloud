{
  "name": "@kotoba-lang/app-kotoba-cloud",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "shadow-cljs release app",
    "test": "clojure -M:test",
    "dry-run": "npm run build && wrangler deploy --dry-run --outdir dist --env=\"\"",
    "deploy": "npm run build && wrangler deploy --env=\"\""
  },
  "devDependencies": {
    "shadow-cljs": "3.5.0",
    "wrangler": "4.127.0"
  }
}

